# Node.js development environment
use flake

# Create a new shell with the specified packages
use nix-shell shell.nix

# Set up Node.js environment
layout node 20  # or specify version: 16, 18, etc.

# Watch .env file
watch_file .env 